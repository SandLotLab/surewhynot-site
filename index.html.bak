<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>SureWhyNot — Social Studio</title>
  <meta name="description" content="SureWhyNot Social Studio — tiny, privacy-forward, ad-friendly mini games + utility." />

  <style>
    :root{
      --bg:#0b0f14;
      --panel:#0f1620;
      --panel2:#0d141d;
      --line:#1e2a3a;
      --text:#e8eef6;
      --muted:#9db0c6;
      --amber:#f5b942;
      --accent:#67b0ff;
      --shadow: 0 10px 30px rgba(0,0,0,.35);
      --radius: 16px;
    }

    *{ box-sizing:border-box; }
    html,body{ height:100%; }
    body{
      margin:0;
      min-height:100vh;
      display:flex;
      flex-direction:column;
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono","Courier New", monospace;
      background: radial-gradient(1200px 600px at 50% 0%, #111a26 0%, var(--bg) 55%);
      color: var(--text);
    }

    a{ color:inherit; text-decoration:none; }

    .wrap{
      width:min(1060px, calc(100% - 32px));
      margin:0 auto;
    }

    header{
      position:sticky;
      top:0;
      z-index:20;
      background: linear-gradient(to bottom, rgba(11,15,20,.95), rgba(11,15,20,.65));
      backdrop-filter: blur(10px);
      border-bottom: 1px solid rgba(30,42,58,.6);
    }

    .topbar{
      padding:16px 0 14px;
      text-align:center;
    }

    .brand{
      display:flex;
      align-items:center;
      justify-content:center;
      gap:10px;
      font-weight:700;
      letter-spacing:.2px;
    }

    .dot{
      width:8px;height:8px;border-radius:999px;
      background: var(--amber);
      box-shadow: 0 0 18px rgba(245,185,66,.45);
    }

    .subtitle{
      margin-top:6px;
      font-size:12px;
      color: var(--muted);
    }

    nav.navcenter{
      display:flex;
      justify-content:center;
      gap:12px;
      flex-wrap:wrap;
      padding: 10px 0 16px;
    }

    .pill{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      padding:10px 14px;
      border-radius:999px;
      border:1px solid rgba(30,42,58,.9);
      background: rgba(15,22,32,.75);
      box-shadow: inset 0 1px 0 rgba(255,255,255,.03);
      transition: transform .08s ease, border-color .12s ease, background .12s ease;
      user-select:none;
    }
    .pill:hover{ transform: translateY(-1px); border-color: rgba(103,176,255,.7); }
    .pill.active{
      border-color: rgba(245,185,66,.95);
      background: rgba(20,28,40,.9);
    }

    main{
      flex:1;
      padding: 24px 0 40px;
    }

    .card{
      background: linear-gradient(180deg, rgba(15,22,32,.92), rgba(13,20,29,.92));
      border:1px solid rgba(30,42,58,.9);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 18px;
    }

    h1{
      margin:0 0 12px;
      font-size: 20px;
      letter-spacing:.3px;
    }

    h2{
      margin:0 0 10px;
      font-size: 14px;
      color: var(--muted);
      font-weight:600;
      letter-spacing:.2px;
    }

    .grid{
      display:grid;
      gap:14px;
      grid-template-columns: 1.2fr .8fr;
    }
    @media (max-width: 880px){
      .grid{ grid-template-columns: 1fr; }
    }

    .muted{ color: var(--muted); }
    .small{ font-size:12px; }

    .textblock{
      white-space:pre-wrap;
      line-height:1.35;
      font-size: 13px;
    }

    .adbox{
      border:1px dashed rgba(245,185,66,.55);
      border-radius: 14px;
      padding:18px;
      text-align:center;
      color: rgba(245,185,66,.95);
      background: rgba(245,185,66,.06);
    }

    footer{
      margin-top:auto;
      border-top: 1px solid rgba(30,42,58,.6);
      padding: 18px 0;
      color: var(--muted);
      font-size: 12px;
    }

    .footerline{
      display:flex;
      justify-content:space-between;
      gap:12px;
      flex-wrap:wrap;
      align-items:center;
    }
  </style>
</head>

<body>

  <header>
    <div class="wrap topbar">
      <div class="brand">
        <span class="dot"></span>
        <span>SureWhyNot — Social Studio</span>
      </div>
      <div class="subtitle">tiny, privacy-forward, ad-friendly</div>

      <nav class="navcenter" aria-label="Site">
        <a class="pill active" href="/index.html">Home</a>
        <a class="pill" href="/typeracer.html">Type Racer</a>
        <a class="pill" href="/puzzle.html">Mini Puzzle</a>
        <a class="pill" href="/utility.html">Utility</a>
      </nav>
    </div>
  </header>

  <main class="wrap">
    <section class="card">
      <h1>Welcome</h1>

      <div class="grid">
        <div class="card" style="box-shadow:none;">
          <h2>What this is</h2>
          <div class="textblock muted">
A small site with simple stuff people actually play:

- Type Racer (repeatable, score-based)
- Mini Puzzle (fast brain snack)
- Utility (your “come back daily” tool slot)

No accounts. No default trackers.
Ads later (your choice).</div>
        </div>

        <div class="card" style="box-shadow:none;">
          <h2>Ad slot placeholder</h2>
          <div class="adbox">[ AdSense Slot Here ]</div>
          <div class="textblock muted small" style="margin-top:10px;">
Keep this spot consistent so layout doesn’t jump later.
          </div>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <div class="wrap footerline">
      <div>© <span id="year"></span> surewhynot.app</div>
      <div class="muted">No accounts • No default trackers</div>
    </div>
  </footer>

  <script>
    document.getElementById("year").textContent = String(new Date().getFullYear());
  </script>

</body>
</html>
